import { ImageDefaultErrorDirective } from './image-default-error.directive';

describe('ImageDefaultErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageDefaultErrorDirective();
    expect(directive).toBeTruthy();
  });
});
